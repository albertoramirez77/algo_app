# ---------------------------------------------------------------------------
# VENDOR DATA — never commit. Databento's licence does not permit redistribution.
# ---------------------------------------------------------------------------
*.parquet
!data/derived/*.parquet
!data/derived/*.csv
batch_downloads/
batch_curve/
batch_curve_c/
batch_curve_c_wide/
cot_raw/
cache/
jobs*.json
.env
*.key

# ---------------------------------------------------------------------------
# COMMITTED ON PURPOSE — these are derived artefacts a reviewer needs, so they
# are deliberately NOT ignored:
#   docs/FINAL_NUMBERS.txt      the canonical run
#   docs/figures/*.png          the exhibits
#   data/derived/*.csv          series sufficient to reproduce every number
# ---------------------------------------------------------------------------

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Local scratch output
baseline_run.txt
baseline_smoke.txt
repair_audit.csv
final_bundle/

# Editor and OS
.DS_Store
**/.DS_Store
.idea/
.vscode/
*.swp
.mcp.json
